global { usage-count no; }

common {
        protocol C;

        startup {
                wfc-timeout  15;
                degr-wfc-timeout 60;
        }

        net {
                cram-hmac-alg sha1;
                shared-secret "secret";
        }

        syncer { rate 500M; }
}
